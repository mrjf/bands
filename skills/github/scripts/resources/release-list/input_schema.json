{
  "type": "object",
  "properties": {
    "repo": { "type": "string", "description": "Repository in owner/name format" },
    "limit": { "type": "integer", "description": "Maximum number of releases to return", "default": 30 }
  },
  "required": ["repo"]
}
