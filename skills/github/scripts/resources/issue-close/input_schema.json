{
  "type": "object",
  "properties": {
    "repo": { "type": "string", "description": "Repository in owner/name format" },
    "number": { "type": "integer", "description": "Issue number" },
    "reason": { "type": "string", "enum": ["completed", "not planned"], "description": "Close reason" },
    "comment": { "type": "string", "description": "Leave a closing comment" }
  },
  "required": ["repo", "number"]
}
