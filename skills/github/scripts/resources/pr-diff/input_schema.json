{
  "type": "object",
  "properties": {
    "repo": { "type": "string", "description": "Repository in owner/name format" },
    "number": { "type": "integer", "description": "Pull request number" },
    "name_only": { "type": "boolean", "description": "Only show names of changed files", "default": false },
    "patch": { "type": "boolean", "description": "Display diff in patch format", "default": false }
  },
  "required": ["repo", "number"]
}
