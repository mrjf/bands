#!/bin/bash
DIR="$(cd -P "$(dirname "$0")" && pwd)"
ROOT="$(cd -P "$DIR/../../.." && pwd)"
SKILL_ROOT="$(cd -P "$DIR/.." && pwd)"
bun "$ROOT/packages/runtime/src/cli.ts" exec "$DIR/resources/label-create" --skill_root "$SKILL_ROOT" "$@"
