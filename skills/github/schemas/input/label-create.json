{
  "type": "object",
  "properties": {
    "repo": {
      "$ref": "repo.json"
    },
    "name": {
      "type": "string",
      "description": "Label name"
    },
    "color": {
      "type": "string",
      "description": "Hex color code (without #)",
      "default": "ededed"
    },
    "description": {
      "type": "string",
      "description": "Label description"
    }
  },
  "required": [
    "repo",
    "name"
  ]
}
