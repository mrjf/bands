name = "band-runtime"
main = "src/worker.ts"
compatibility_date = "2024-01-01"

[vars]
# BAND_CONFIG will be set during deployment with the band definition

[build]
command = "bun build src/worker.ts --outdir dist --target browser --minify"
