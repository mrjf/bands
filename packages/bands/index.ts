/**
 * @bands/bands - Placeholder for curated band definitions.
 *
 * BANDS_DIR points to this package's root directory.
 * Band definitions are added as subdirectories, each containing a BAND.md file.
 */

export const BANDS_DIR = new URL(".", import.meta.url).pathname;
